apiVersion: codefresh.io/v1beta1
kind: PromotionFlow
metadata:
  name: e2e-test
  annotations:
    description: ""
spec:
  triggerEnvironment: dev
  hooks:
    onSuccess: test
  steps:
    - environment: integration
      dependsOn:
        - dev
      policy:
        action: commit
